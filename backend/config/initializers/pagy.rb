# Note: Pagy 43.x DEFAULT is frozen, so we configure via Pagy::DEFAULT
# We create our own defaults for manual pagination in PaginationHelper
# Pagy will use its internal defaults unless overridden per-instance
